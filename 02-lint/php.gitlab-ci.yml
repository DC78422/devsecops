include:
  - local: 'functions.gitlab-ci.yml'

php-lint:
  extends:
    - ".lint_base"
    - ".lint_rule"
  variables:
    LINT_IMAGE: harbor.example.com/devops/golangci-lint:latest
    KUBERNETES_SERVICE_ACCOUNT_OVERWRITE: "builder"
  script:
    - echo "Nothing"
  when: manual
