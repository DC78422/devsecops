include:
  - local: 'functions.gitlab-ci.yml'

Forming CycloneDX SBOM:
  extends:
    - ".artifacts"
    - ".audit_base"
    - ".audit_rule"
  image:
    name: registry.gitlab.com/dc20e6/devsecops/images/cyclonedx-php:latest
    entrypoint: [""]
  before_script:
    - echo "composer_before_Script"
  script:
    - echo "composer_script"
    # - composer CycloneDX:make-sbom 
    #   --output-format=JSON
    #   --output-file=reports/cdx-sbom.php.json
  # after_script:
  #   - |
  #     if [ "$DT_UPLOAD_REPORT" = 'true' ]; then
  #       echo "Uploading"
  #       dt-wrapper --dt-sbom-path reports/cdx-sbom.php.json --dt-enable-project-properties
  #     fi
  when: manual

# DefectDojo Engagement Creation:
#   extends:
#     - ".DefectDojo_Engagement_Creation"
